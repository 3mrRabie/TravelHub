org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Fix for Kotlin incremental compiler crash on Windows when project (E:) and
# Pub cache (C:) are on different drives. Kotlin cannot compute relative paths
# across drive roots, causing IllegalArgumentException in the Kotlin daemon.
# See: https://youtrack.jetbrains.com/issue/KT-43288
kotlin.incremental=false
